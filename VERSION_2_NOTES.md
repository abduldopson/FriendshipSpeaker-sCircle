# Friendship Speaker’s Circle — Version 2

Added:
- Global site search (Search button + Ctrl/Cmd+K)
- “Ask the Club” member assistant with built-in offline club FAQ fallback
- Secure Vercel `/api/chat` function ready for `OPENAI_API_KEY`
- 10-question “Find Your Path” Pathways Identifier with primary + secondary recommendation
- Expanded Meeting Roles with sample introductions, scripts, reports, and copy buttons
- Member Hub section explaining the new tools

Important: AI generative responses require an `OPENAI_API_KEY` environment variable in Vercel. Without it, the assistant still answers common club questions from its built-in knowledge.
